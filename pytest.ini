[pytest]
markers =
    integration: requires PytorchWildlife installed and model download (~500MB)

# CVE-2025-71176: pytest <=9.0.2 uses predictable /tmp/pytest-of-{user} paths.
# No patched version exists yet. Mitigate by setting PYTEST_DEBUG_TEMPROOT
# to a user-owned directory with restricted permissions.
# e.g. export PYTEST_DEBUG_TEMPROOT="$HOME/.pytest-tmp"
